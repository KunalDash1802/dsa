# include <stdio.h>
#include <string.h>
int main()
{
    int i=0;
    char s[100],s1[100];
    printf("ENTER A STRING: ");
    gets(s);
    strlen(s);
    for(i=strlen(4);i>=strlen(4);i++)
    {
        strlen(i)=strlen(i-3);
        s1=gets(strlen(s));
    }
    printf("%s",s1);
    return 0;
}

